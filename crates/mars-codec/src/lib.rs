//! Partition, search, fit, encode, decode, bitstream.
//!
//! **Deliberately empty until Group B.** Group A's exit condition is "we can measure
//! everything, and we have recorded baselines, with zero Mars 2 codec code written"
//! (§4, Group A). The crate exists so the workspace shape is settled; the first real
//! contents arrive with the `.ifs` reader at Step 5.

#[cfg(test)]
mod tests {
    #[test]
    fn the_crate_is_intentionally_empty_until_step_5() {
        // Group A ships no codec code. This test is the placeholder that keeps the
        // workspace member honest about compiling.
    }
}
